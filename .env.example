# Copy to `.env` locally (never commit `.env`)
GEMINI_API_KEY=

# Optional overrides
# COGNIAUDIT_EMBEDDING_MODEL=text-embedding-004
# COGNIAUDIT_AUDIT_MODEL=gemini-1.5-flash

# Offline / zero-cost self-check mode (uses deterministic fake embeddings + JSON)
# COGNIAUDIT_USE_FAKE=1
